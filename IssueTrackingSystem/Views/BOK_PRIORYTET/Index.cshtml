@model IEnumerable<IssueTrackingSystem.Models.BOK_PRIORYTET>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Priorytety</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OPIS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OPIS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_PRIORYTET }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_PRIORYTET }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_PRIORYTET })
        </td>
    </tr>
}

</table>
